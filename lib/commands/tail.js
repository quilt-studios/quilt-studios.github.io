(()=>{'use strict';const S=POSIXShell,U=S.fsutil;S.register('tail',async({a})=>{U.need(a,'usage: tail [-n N] FILE');let n=10;if(a[0]==='-n'){n=Number(a[1]);a=a.slice(2)}if(!Number.isFinite(n)||n<0)throw Error('invalid line count');const[,f]=U.node(a[0]);if(f.t==='d')throw Error('is a directory');const text=f.t==='b'?new TextDecoder().decode(f.d):String(f.d||'');S.out(text.split('\n').slice(-n).join('\n'))});})();
