(()=>{'use strict';const S=window.POSIXShell,M=window.POSIXLabsMobile;S.register('labs',async({a})=>{const op=a.shift()||'list';if(op==='list'){S.out('Beta features:');S.out('mobilemode  '+(M?.enabled()?'enabled':'disabled')+'  Custom mobile shell keyboard with cursor navigation, symbols, modifiers and shortcuts');return}if(op==='status'){S.out('mobilemode: '+(M?.enabled()?'enabled':'disabled'));return}if(op==='mobilemode'){if(!M)throw Error('mobilemode subsystem unavailable');const action=a.shift()||'status';if(action==='on'||action==='enable'){M.apply(true);S.out('labs mobilemode: enabled');return}if(action==='off'||action==='disable'){M.apply(false);S.out('labs mobilemode: disabled');return}if(action==='status'){S.out('mobilemode: '+(M.enabled()?'enabled':'disabled'));return}throw Error('usage: labs mobilemode [on|off|status]')}throw Error('usage: labs [list|status|mobilemode [on|off|status]]')});})();
