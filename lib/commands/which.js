(()=>{'use strict';const S=POSIXShell,P=S.P;S.register('which',async({a})=>{if(!a.length)throw Error('usage: which COMMAND...');for(const c of a){if(S.commands.has(c)){S.out('/bin/'+c);continue}const paths=[c.startsWith('/')?c:'/bin/'+c,'/usr/bin/'+c,'/sbin/'+c,'/usr/sbin/'+c],p=paths.find(x=>P.fs.has(P.norm(x)));if(p)S.out(P.norm(p));else throw Error(c+': not found')}});})();
