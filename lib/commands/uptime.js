(()=>{'use strict';const S=POSIXShell,P=S.P;S.register('uptime',async()=>{let s=Math.floor((Date.now()-P.boot)/1000),d=Math.floor(s/86400);s%=86400;const h=Math.floor(s/3600);s%=3600;const m=Math.floor(s/60),sec=s%60;S.out('up '+(d?d+' day'+(d===1?'':'s')+', ':'')+[h,m,sec].map(x=>String(x).padStart(2,'0')).join(':'))});})();
