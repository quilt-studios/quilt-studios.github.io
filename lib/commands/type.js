(()=>{'use strict';const S=POSIXShell,P=S.P;S.register('type',async({a})=>{if(!a.length)throw Error('usage: type COMMAND...');for(const c of a){if(S.commands.has(c)){S.out(c+' is a shell builtin');continue}const paths=[c.startsWith('/')?c:'/bin/'+c,'/usr/bin/'+c,'/sbin/'+c,'/usr/sbin/'+c],p=paths.find(x=>P.fs.has(P.norm(x)));if(p)S.out(c+' is '+P.norm(p));else throw Error(c+': not found')}});})();
