(()=>{'use strict';const S=POSIXShell;S.register('seq',async({a})=>{if(a.length<1||a.length>3)throw Error('usage: seq [FIRST [STEP]] LAST');let first=1,step=1,last;if(a.length===1)last=Number(a[0]);else if(a.length===2){first=Number(a[0]);last=Number(a[1])}else{first=Number(a[0]);step=Number(a[1]);last=Number(a[2])}if(![first,step,last].every(Number.isFinite)||step===0)throw Error('invalid number');const vals=[],limit=10000;if(step>0)for(let x=first;x<=last&&vals.length<limit;x+=step)vals.push(x);else for(let x=first;x>=last&&vals.length<limit;x+=step)vals.push(x);if(vals.length===limit)throw Error('sequence too large');S.out(vals.join('\n'))});})();
