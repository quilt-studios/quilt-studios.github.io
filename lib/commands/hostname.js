(()=>{'use strict';const S=POSIXShell,P=S.P;S.register('hostname',async({a,root})=>{const f=P.fs.get('/etc/hostname');if(!a.length)return S.out(String(f?.d||'posixjs').trim());if(a.length!==1)throw Error('usage: hostname [NAME]');if(!root)throw Error('permission denied: use sudo hostname NAME');if(!/^[A-Za-z0-9.-]{1,63}$/.test(a[0]))throw Error('invalid hostname');P.write('/etc/hostname',a[0]+'\n');S.out(a[0])});})();
