(()=>{'use strict';const S=POSIXShell,P=S.P,U=S.fsutil;S.register('mv',async({a})=>{if(a.length!==2)throw Error('usage: mv SOURCE DEST');const[sp,sn]=U.node(a[0]);let dest=P.norm(a[1]);const dn=P.fs.get(dest);if(dn?.t==='d')dest=P.norm(dest+'/'+sp.split('/').pop());if(dest===sp)return;if(sn.t==='d'&&dest.startsWith(sp+'/'))throw Error('cannot move a directory into itself');U.copyTree(sp,dest);U.removeTree(sp)});})();
