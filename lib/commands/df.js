(()=>{'use strict';const S=POSIXShell,P=S.P,U=S.fsutil;S.register('df',async({a})=>{if(a.length)throw Error('usage: df');const used=U.sizeOf('/'),limit=256*1024*1024,free=Math.max(0,limit-used),pct=Math.min(100,Math.round(used/limit*100));S.out('Filesystem       Size       Used      Avail Use% Mounted on');S.out('indexeddb       '+P.fmt(limit).padStart(8)+' '+P.fmt(used).padStart(10)+' '+P.fmt(free).padStart(10)+' '+String(pct).padStart(3)+'% /')});})();
